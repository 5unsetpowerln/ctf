import { $ } from "bun";

async function run() {
  // const output = await $`echo ${msg}`;
  const output = await $`echo hello`;
  // console.log(output.text());
  // console.log(output.text())
}

run();
// run("hello^.*");
